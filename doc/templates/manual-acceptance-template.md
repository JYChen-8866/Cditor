# Manual Acceptance：<验收主题>（YYYY-MM-DD）

> 关联任务：PN-NNN / Gate PN
>
> 分支与提交：`<branch>` @ `<commit>`（含未提交改动时注明）
>
> 结论只允许三种：通过 / 不通过 / 未执行。未执行不得记为通过。

## 1. 环境

| 项 | 值 |
|---|---|
| 设备型号 | |
| OS 与版本（含 build） | |
| 显示配置（分辨率 / scale / 多屏） | |
| 输入法与版本（IME 验收必填） | |
| 应用 profile（dev/release）与启动方式 | |
| 测试账号 / 数据 fixture 版本 | |

## 2. 用例矩阵

每个用例必须有独立证据；录像或截图文件放入
`doc/acceptance/evidence/<日期>-<主题>/` 并在表中引用相对路径。

| 编号 | 步骤 | 预期 | 实际 | 结论 | 证据（录像/截图） |
|---|---|---|---|---|---|
| M-001 | | | | 通过/不通过/未执行 | |
| M-002 | | | | | |

## 3. 缺陷记录

| 编号 | 关联用例 | 现象 | 严重级别 | 处理（issue/任务） |
|---|---|---|---|---|

## 4. 结论

- 通过用例：N / 总数 M；未执行：K（逐条列出未执行原因）。
- Gate 判断：该验收（支持 / 不支持）勾选对应任务，理由：
- 已知残留风险：
