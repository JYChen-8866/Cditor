[package]
name = "cditor-collaboration"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cditor-core = { path = "../cditor-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
