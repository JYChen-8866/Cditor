[package]
name = "cditor-config"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[build-dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
